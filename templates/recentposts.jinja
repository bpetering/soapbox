<h3>Recent Posts</h3>
<ul class="recent-posts">
    {% for post in posts[:5] %}
    <li>
        <article class="post">
            <a href="{{ post.url }}">{{ post.title }}</a>
            <span class="when-posted">{{ post.date }}</a>
        </article>
    </li>
{% endfor %}
</ul>

