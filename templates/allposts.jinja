<h3>All Posts</h3>
<ul class="all-posts">
    {% for post in posts %}
    <li>
        <article class="post">
            <a href="{{ post.url }}">{{ post.title }}</a>
            <span class="when-posted">{{ post.date }}</a>
        </article>
    </li>
{% endfor %}
</ul>

